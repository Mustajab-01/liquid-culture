import { LiquidList, SearchForm } from '../components';

const HomePage = () => {
    return (
        <div>
            <SearchForm />
            <LiquidList />
        </div>
    );
};

export default HomePage;
